# 三步安装（一句话版失败时用）

分三段发，每段都能看清哪一步出错。

```
第一步：把仓库克隆到 ~/.understd，跑 install.sh，把自检输出贴给我。
```
```
第二步：在你的规则文件里加一条：交付前先跑 ~/.understd/check.py。
```
```
第三步：用一段长句试跑，把输出给我看。
```

## 哪些智能体装得了

要能读写文件、能跑命令。终端里的编码智能体基本都行。
纯网页对话窗口不行——它不能写文件，也就没法落地。
那种情况只能给提示词包，硬检查那层拿不到。
